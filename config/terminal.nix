# terminal and toggleterm configuration
{
  config.vim.terminal = {
    toggleterm = {
      enable = true; # enable floating terminal
      lazygit.enable = false; # lazygit can be toggled manually if needed
    };
  };
}
