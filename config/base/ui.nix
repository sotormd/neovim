{
  config.vim = {

    theme = {
      name = "nord";
      style = "dark";
      enable = true; # enable theme management
      transparent = false; # disable transparency for better contrast
    };

  };
}
