# theme and color scheme configuration
{
  config.vim.theme = {
    enable = true; # enable theme management
    name = "nord"; # set preferred theme
    style = "dark"; # use dark variant
    transparent = false; # disable transparency for better contrast
  };
}
